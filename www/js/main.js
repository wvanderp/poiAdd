//the main js that starts everything
$( document ).ready(function(){
	//makes all the event listeners 
	uiEvents();
	//loads the map
	initMap();

	// debug
	// hideMap();
	// addInfoCollectWindow();
})